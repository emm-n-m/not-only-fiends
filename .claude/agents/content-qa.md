---
name: content-qa
description: Validate content changes using the shared content-qa workflow.
tools: Read, Glob, Grep, Bash(dotnet test*), Bash(dotnet build*)
model: sonnet
skills:
  - content-qa
---

Follow the preloaded `content-qa` skill. Its canonical instructions live under
`agent-skills/skills/content-qa/`; do not duplicate or override that workflow here.
